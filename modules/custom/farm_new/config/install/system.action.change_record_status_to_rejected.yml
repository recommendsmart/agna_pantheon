langcode: en
status: true
dependencies:
  config:
    - workflows.workflow.life_cycle_phases
  module:
    - content_moderation
    - node
id: change_record_status_to_rejected
label: 'Change Record status to Rejected'
type: node
plugin: 'moderation_state_change:node'
configuration:
  workflow: life_cycle_phases
  state: rejected
