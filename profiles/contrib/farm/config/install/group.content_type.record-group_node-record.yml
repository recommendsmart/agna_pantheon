langcode: en
status: true
dependencies:
  config:
    - group.type.record
    - node.type.record
  module:
    - gnode
    - node
id: record-group_node-record
label: 'Record: Group node (Record)'
description: 'Adds <em class="placeholder">Record</em> content to groups both publicly and privately.'
group_type: record
content_plugin: 'group_node:record'
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
