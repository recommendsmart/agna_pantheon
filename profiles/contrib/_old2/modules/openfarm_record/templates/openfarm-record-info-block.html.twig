{#
/**
* @file
* Default theme implementation to display a block.
*
* @see template_preprocess_block()
*
* @ingroup themeable
*/
#}
<div class="record-information">
    <ul class="record-information--list">
        {% for item in content %}
            {% if item.access %}
                <li class="record-information--list__item">
                    <div class="record-information--list__item--title">{{ item.title }}</div>
                    <div class="record-information--list__item--value">{{ item.value }}</div>
                </li>
            {% endif %}
        {% endfor %}
    </ul>
</div>