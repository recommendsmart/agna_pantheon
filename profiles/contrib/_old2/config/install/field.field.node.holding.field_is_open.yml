langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_is_open
    - node.type.holding
id: node.holding.field_is_open
field_name: field_is_open
entity_type: node
bundle: holding
label: Open
description: "Check or uncheck this to mark the current holding as open or close.\r\nChallenges without \"Open\" option are still published but they are closed for further activity."
required: false
translatable: false
default_value:
  -
    value: 1
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
