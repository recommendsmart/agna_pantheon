langcode: en
status: true
dependencies:
  config:
    - group.type.record
  module:
    - user
id: record-group_membership
label: 'Idea: Group membership'
description: 'Adds users to groups as members.'
group_type: record
content_plugin: group_membership
plugin_config:
  group_cardinality: 0
  entity_cardinality: 1
  use_creation_wizard: false
